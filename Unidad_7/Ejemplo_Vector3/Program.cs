namespace Ejemplo_Vector3;

class Program
{
    static void Main(string[] args)
    {
        // VECTORES CON BOOL

        // los vectores que conocemos son INT, DOUBLE, BOOL, CHAR

        int[] legajos = new int[100];
        bool[] asistencia = new bool[100];

        for (int i = 0; i < 16; i++)
        {
            for (int x = 0; x < 100; x++)
            {
                // aca puedo jugar con 16 lotes de 100 personas cada uno
            }
        }
        




        
        

    }
}
